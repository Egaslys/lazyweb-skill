---
name: lazyweb-optimize-sign-up
description: |
  DEPRECATED — moved. Sign-up / registration optimization is now handled by the
  lazyweb-design umbrella. Do not run it; route to the new skill.
---

# Deprecated — moved

This skill has moved. Optimizing a sign-up / registration screen is now handled
by **`/lazyweb-design`** (optimize a signup screen).

1. Run `/lazyweb-update` to refresh your local Lazyweb skill pack.
2. Use `/lazyweb-design` instead of this skill.
