{
  "name": "@lazyweb/browse",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Lazyweb headless-browser CLI used by the lazyweb-* design skills. Runtime deps are declared so bun install produces a resolvable node_modules and the tool runs offline (serverless/CI) without bun runtime auto-install.",
  "dependencies": {
    "diff": "^9.0.0",
    "playwright": "^1.61.1",
    "playwright-core": "^1.61.1"
  }
}
